module.exports = (client) => {
    /*
    function jc_smoothie_support_server_roles(){
        channelId = '780098106071056384';
        const updateMembers = (guild) => {
            const channel = guild.channels.cache.get(channelId);
            channel.setName(`Members: ${guild.memberCount.toLocaleString()}`);
        }
    
        client.on('guildMemberAdd', (member) => updateMembers(member.guild));
        client.on('guildMemberRemove', (member) => updateMembers(member.guild));
    
        const guild = client.guilds.cache.get('755814665111470142');
        updateMembers(guild);
    }

    function Not_Identified_Server(){
        channelId = '';
    }
    
    if(client.guild.id == jc_smoothie_support_server){
        jc_smoothie_support_server_roles();
    } else{
        Not_Identified_Server();
    }
    */

    /*
    const channelId = '780098106071056384';

    
    const updateMembers = (guild) => {
        const channel = guild.channels.cache.get(channelId);
        channel.setName(`Members: ${guild.memberCount.toLocaleString()}`);
    }

    client.on('guildMemberAdd', (member) => updateMembers(member.guild));
    client.on('guildMemberRemove', (member) => updateMembers(member.guild));

    const guild = client.guilds.cache.get('755814665111470142');
    updateMembers(guild);
    */
}